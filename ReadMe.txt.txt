Good day. To run this project follow next steps

1. To run this project You must run Json server (db.json) in port 4000 ($ json-server --watch db.json --port 4000). If you have not installed Json server install in your computer Install it -- npm i json-server -g 
2. Open project and npm install
3. npm start
Enjoy
